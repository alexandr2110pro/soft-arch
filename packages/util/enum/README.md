# util-enum

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-enum` to build the library.

## Running unit tests

Run `nx test util-enum` to execute the unit tests via
[Vitest](https://vitest.dev/).
